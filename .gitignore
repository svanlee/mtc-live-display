# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Node (only used by CI/manifest tooling — no build step for the site itself)
node_modules/
npm-debug.log*

# Local env / secrets (never commit real API keys)
.env
*.local.js

# Logs
*.log
